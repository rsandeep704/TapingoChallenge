Requirements:
1. Python 2.x
2. twitter-python 3.3

To deploy the app in a local setup (localhost), use the following steps:
1. Download the project directory to a local directory
2. Update the twitter consimer keys in the seetings.py file in the project's root directory
3. Open the terminal and navigate to the project's root directroy
4. Run the following commands in the same order:
    a. pip install -r requirements.txt
    b. python manage.py makemigrations
    c. python manage.py migrate
    d. python manage.py runserver


Now, the server should be started at port 8000. Navigate to the url http://localhost:8000/ to use the app